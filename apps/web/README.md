# apps/web — Next.js (App Router) + Tailwind

Phase 4. Single global left nav (14 items), Settings reachable from every screen,
fraud/capacity merged into Risk Review. Score direction corrected in every view
(HIGH = GOOD). Enforcement = confirm-with-reason dialog; dispatch-block toggle reads
`FEATURE_DISPATCH_BLOCK_ENFORCING` (dormant by default). No browser storage.
