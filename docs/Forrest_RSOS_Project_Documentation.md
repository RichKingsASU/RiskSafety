# Forrest RSOS — Project Documentation (Table Dictionary · RBAC Matrix · SOPs)

> ⚠️ **PLACEHOLDER — the real document was not provided.**
>
> This is the source of truth for:
> - the **table dictionary** (needed to generate the remaining ~22 tables in Phase 1),
> - the **RBAC matrix** (needed to write the ten-role RLS policies), and
> - the **SOPs**.
>
> It is **missing**. Phase 1 (schema + RLS) cannot proceed faithfully without it,
> and `CLAUDE.md` forbids inventing this context.
>
> **Action:** replace this file with the real Project Documentation, then re-run the
> kickoff prompt. See `docs/STATUS.md`.
