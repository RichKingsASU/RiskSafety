# Forrest RSOS — Implementation Plan

> ⚠️ **PLACEHOLDER — the real document was not provided.**
>
> `CLAUDE.md` and `docs/Forrest_RSOS_Claude_Code_Build_Prompt.md` both require this
> file to be read in full before building. It is **missing**. This stub exists only
> so the path resolves and the gap is obvious.
>
> **Action:** replace this file with the real Implementation Plan, then re-run the
> kickoff prompt. See `docs/STATUS.md`.
